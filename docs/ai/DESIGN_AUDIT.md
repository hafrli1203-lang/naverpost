# DESIGN_AUDIT — naverpost

> 현재 UI 문제와 향후 점검 기준. 한 화면씩 점검한다.

## 점검 기준 체크리스트 (화면마다)
- [ ] 현재 단계/다음 행동이 보이는가(스테퍼·진입 카드)
- [ ] 주요 CTA가 1~2개로 정리됐는가
- [ ] 버튼명이 행동 중심인가(키워드 생성/본문 작성/이미지 생성/export 복사)
- [ ] 로딩(스켈레톤)/빈/오류 상태 3종이 있는가(AI·API 실패 포함)
- [ ] 검증 결과(금지어/규칙 위반)가 눈에 띄고 수정 동선이 있는가
- [ ] 반응형 대응되는가
- [ ] 키보드/대비/ARIA 접근성 + 다크모드 OK인가
- [ ] AI Tells(흔한 그라데이션/제네릭 패턴/이모지) 없는가

## 현재 관찰 (구조 기준 가설 — 실제 렌더로 검증 필요)
- 화면: `operations`(운영 파이프라인), `admin`, `login`.
- 3단계 파이프라인 → 스테퍼/단계 상태·전환 동선 점검 우선.
- AI 호출 길이 → 로딩/진행 표시·타임아웃 오류 처리 점검 필요.

## 다음 점검 대상
1. operations 메인(진입/스테퍼)
2. 키워드 선택 + 본문 미리보기(검증 가시화)
3. 이미지 미리보기 + export
(각 화면은 실제 :3100 렌더 후 스크린샷 기준으로 audit 갱신)
