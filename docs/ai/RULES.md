# RULES — naverpost

상위: `C:\project\_AGENCY_OS\MASTER_RULES.md` + **기존 `CLAUDE.md`·`bot_created_rule.md`(최우선)**.

## 반드시 지킬 기존 규칙
- 네이버 블로그에 **절대 발행하지 않는다**(자동 발행 API 없음). 산출물은 붙여넣기 export까지.
- 한국어 전용 콘텐츠. 본문 ~2000자, 제목 12~32자, 키워드 2단어 조합.
- 이모지/번호목록 금지. 쉼표: 본문 제한적 허용(남발 금지), 제목 무콤마.
- 광고법/의료법 준수: 금지어 100+ 자동 필터링. 6개 매장 콘텐츠 중복 방지.
- bot_created_rule.md 5단계 플로우(설계→스펙 테스트→아웃풋 체크→인풋 체크→모듈화) 준수.
- `CLAUDE.md`의 "현재 모듈 상태 경계"를 침범하지 않는다.

## 하면 안 되는 것
- 패키지 설치, 배포, DB 마이그레이션, git commit/push, 파일/폴더 삭제.
- 기존 기능·API 라우트·모듈 제거, 대량 리팩토링.
- `.env.local`/API 키·토큰·쿠키/네이버 계정 자격증명/고객·매장 정보 열람·출력.

## 검수 기준
- `pnpm dev`(3100) / `pnpm build` / `pnpm type-check` / `pnpm test` 에러 없음.
- 변경 동작이 실제로 확인됨(스크린샷·테스트 출력 등 증거).
- 기존 기능 회귀 없음. 엣지 케이스(빈 입력, API 오류, 로딩) 처리.
