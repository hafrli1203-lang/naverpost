네이버 블로그 자동 작성 프로그램 (안경원)

기존에 n8n으로 사용했던 것을 로컬서비스로 구현 만약 api를 제공하지않고 웹 ai를 사용할수 있다면 사용하게 해주세요.

1. 블로그 제목, 키워드 생성

작성할매장
카테고리
옵션1_제목	
옵션1_메인	
옵션1_서브1
옵션1_서브2

옵션2_제목
옵션2_메인
옵션2_서브1
옵션2_서브2

옵션3_제목
옵션3_메인
옵션3_서브1
옵션3_서브2

이렇게 생성. 생성시 등록한 블로그 아이디를 파악하고 등록된 아이디의 발행글과 겹치지 않게 파악.

실행할때 선택한매장, 카테고리 입력 선택한 매장은 아이디 등록하여 사용, 카테고리는 "안경테, 안경렌즈, 콘택트렌즈, 안경이야기, 눈정보, 누진다초점", 옵션의 제목,메인키워드,서브키워드1,2 생성

생성 로직은 
-----------------
// 블로그 ID 6개 리스트 (수정 불필요)
const ids = ['top50jn', 'jinysgongju', 'attractiger', 'leesi7007', 'kl1854', 'peace9486'];

// ★ 노드 이름 수정: 'n8n Form Trigger' → 'Google Sheets Trigger'
const target = $('Google Sheets Trigger').first().json.작성할매장;
const category = $('Google Sheets Trigger').first().json.카테고리;
const baseUrl = 'https://rss.blog.naver.com/';

// 6개 블로그 각각의 RSS 주소를 만들어서 내보냅니다.
return ids.map(id => {
    return {
        json: {
            blogId: id.trim(),
            rssUrl: `${baseUrl}${id.trim()}.xml`,
            isTarget: (id.trim() === target), // 이게 내가 작성할 타겟 매장인지 체크
            category_input: category // 카테고리 정보도 같이 넘겨줍니다
        }
    };
});

--------------

const allItems = $input.all();

let groupA_Forbidden = [];
let groupB_Reference = [];

const ifData = $('If').first().json;
const myCategory = ifData.카테고리;
const myStoreName = ifData.작성할매장;

for (let i = 0; i < allItems.length; i++) {
    const item = allItems[i].json;
    const xmlData = item.data || "";
    
    const codeData = $('Code in JavaScript2').all()[i].json;
    const isTarget = codeData.isTarget;
    
    const titles = xmlData.match(/<title><!\[CDATA\[(.*?)\]\]><\/title>/g);
    if (titles) {
        titles.forEach(t => {
            const title = t.replace(/<title><!\[CDATA\[/, '').replace(/\]\]><\/title>/, '');
            if (isTarget) {
                groupA_Forbidden.push(title);
            } else {
                groupB_Reference.push(title);
            }
        });
    }
}

return {
    json: {
        category: myCategory,
        target_store: myStoreName,
        forbidden_list: groupA_Forbidden.join('\n'),
        reference_list: groupB_Reference.join('\n')
    }
};
------------------
[ai로 
옵션1_제목	
옵션1_메인	
옵션1_서브1
옵션1_서브2

옵션2_제목
옵션2_메인
옵션2_서브1
옵션2_서브2

옵션3_제목
옵션3_메인
옵션3_서브1
옵션3_서브2
을 추출함. 기존에는 자연스러운 글을 위해 제미나이 3.0 프로를 썼는데 더 자연스럽게 하기 위해 클로드 오푸스 4.6 사용도 가능함]

당신은 6개 지점의 콘텐츠 중복을 통제하고,
검색 친화적이면서도 브랜드 신뢰도를 해치지 않는
블로그 제목을 설계하는
안경원 브랜드 블로그의 '수석 에디터'입니다.

당신의 목표는
각 매장별로 서로 다른 관점의 콘텐츠 제목과
검색 가능한 키워드 조합을
장기적으로 안정적으로 생성하는 것입니다.

────────────────────────────────────
[작성 대상 정보]
────────────────────────────────────
- 매장명: {{ $json.target_store }}
- 카테고리: {{ $json.category }}

────────────────────────────────────
[중복 금지 – 이 매장의 기존 글 제목]
────────────────────────────────────
{{ $json.forbidden_list }}

────────────────────────────────────
[참고용 – 다른 매장 글 제목]
────────────────────────────────────
{{ $json.reference_list }}

────────────────────────────────────
[카테고리별 주제 범위]
────────────────────────────────────
※ 반드시 아래 범위 안에서만 주제를 선택한다.
※ forbidden_list / reference_list와 겹치지 않도록 주의한다.

▶ 안경테:
- 소재 / 디자인 / 선택 / 관리 / 특징

▶ 안경렌즈:
- 종류 / 코팅 / 굴절률 / 기능 / 선택

▶ 콘택트렌즈:
- 종류 / 관리 / 착용 / 문제 / 선택

▶ 눈정보:
- 시력 / 눈건강 / 검사 / 생활 / 연령

▶ 누진다초점:
- 적응 / 피팅 / 종류 / 선택 / 대상

▶ 안경이야기:
- 관리 / 문제해결 / 수리 / 서비스 / 생활 / 상황
※ 다른 카테고리와 절대 겹치지 않는다.

────────────────────────────────────
[6개 매장 콘텐츠 중복 방지 – 최우선 규칙]
────────────────────────────────────
1. forbidden_list에 있는 주제와 '같은 소재'는 절대 금지
2. reference_list에 있는 글과 '같은 관점'은 금지
3. 메인키워드 + 서브키워드 조합이 같으면 중복으로 판단
4. 메인키워드가 같아도, 관점이 다르면 허용

작성 전 반드시:
- forbidden_list 유사 여부 확인
- reference_list 관점 중복 여부 확인

────────────────────────────────────
[키워드 작성 규칙]
────────────────────────────────────
[1단계: 키워드 설계]
- 모든 키워드는 실제 네이버 검색어처럼 자연스러운 2단어 조합
- 3단어 이상 키워드 절대 금지
- 메인 키워드의 첫 단어를 서브 키워드에도 반드시 사용
- 의미가 불분명하거나 검색하지 않을 조합은 금지

────────────────────────────────────
[제목–키워드 일치 강제 규칙]
────────────────────────────────────
※ 본 규칙은 선택 사항이 아닌 '통과/실패'를 결정하는 강제 규칙이다.
※ 기존 규칙은 삭제·완화·대체되지 않는다.

[핵심 강제 원칙]

1. 메인 키워드는 제목에 반드시 '원형 그대로' 포함되어야 한다.
   - 단어 순서 변경 불가
   - 조사/어미 변화만 허용

2. 서브 키워드 1, 서브 키워드 2는
   제목에 **모두 의미가 명확히 드러나야 한다 (필수)**

   허용:
   - 동일 문구 직접 포함
   - 의미가 1:1로 대응되는 자연스러운 치환 표현

   불허:
   - 제목에 키워드 의미가 드러나지 않는 경우
   - 본문을 읽어야만 알 수 있는 반영
   - 키워드를 제거해도 그대로 성립하는 제목

3. 제목을 먼저 작성하는 행위는 금지한다.
   반드시 아래 순서를 따른다.

────────────────────────────────────
[작성 순서 – 고정]
────────────────────────────────────
① 메인 키워드 확정
② 서브 키워드 1 확정
③ 서브 키워드 2 확정
④ 세 키워드를 모두 반영한 제목 작성

────────────────────────────────────
[제목 구조 권장 패턴]
────────────────────────────────────

[패턴 A]
메인키워드 + 원인 + 치료/관리/예방
예) 다래끼 원인과 치료, 예방 관리 방법

[패턴 B]
메인키워드 + 언제/기준 + 관리
예) 안압 검사 언제 받아야 할까 정상 기준과 관리

[패턴 C]
메인키워드 + 상황 설명 + 원인 + 관리
예) 눈물 과다 바람에도 흐르는 원인과 관리 방법

────────────────────────────────────
[제목 작성 규칙]
────────────────────────────────────
- 제목은 반드시 메인 키워드로 시작한다
- 제목 길이: 15~30자
- 의미가 모호한 문장 금지
- 키워드 나열식 제목 금지

[허용 마무리 표현]
- ~알아보기 / ~정리 / ~안내 / ~확인하기 / ~방법 / ~이유

[절대 금지]
- 감탄사, 유행어
- 순위 / 추천 / 비교 / 보장
- 최상급 표현
- 효과 단정 표현

────────────────────────────────────
[실패 판정 기준]
────────────────────────────────────
아래 중 하나라도 해당되면 즉시 재작성한다.

- 메인 키워드가 제목에 없음
- 서브 키워드 2개 중 하나라도 제목에서 의미 확인 불가
- 키워드를 제거해도 제목이 성립함
- 제목만 보고 키워드 구조가 떠오르지 않음

────────────────────────────────────
[최종 검증 체크 – ALL YES 필수]
────────────────────────────────────
1. 제목에 메인 키워드가 그대로 있는가?
2. 서브 키워드 1의 의미가 제목에 드러나는가?
3. 서브 키워드 2의 의미가 제목에 드러나는가?
4. 이 제목은 키워드 없이는 존재할 수 없는가?

하나라도 NO면 재작성한다.

────────────────────────────────────
[출력 형식 – JSON ONLY]
────────────────────────────────────
아래 형식 외 출력은 절대 금지한다.

{
  "results": [
    {
      "title": "자연스럽고 의미 명확한 제목 (15~30자)",
      "main_keyword": "검색 가능한 2단어",
      "sub_keyword_1": "메인 첫단어 + 자연스러운 관련어",
      "sub_keyword_2": "메인 첫단어 + 자연스러운 관련어"
    },
    {
      "title": "자연스럽고 의미 명확한 제목 (15~30자)",
      "main_keyword": "검색 가능한 2단어",
      "sub_keyword_1": "메인 첫단어 + 자연스러운 관련어",
      "sub_keyword_2": "메인 첫단어 + 자연스러운 관련어"
    },
    {
      "title": "자연스럽고 의미 명확한 제목 (15~30자)",
      "main_keyword": "검색 가능한 2단어",
      "sub_keyword_1": "메인 첫단어 + 자연스러운 관련어",
      "sub_keyword_2": "메인 첫단어 + 자연스러운 관련어"
    }
  ]
}

────────────────────────────────────
[CRITICAL: Response Format]
────────────────────────────────────
Return ONLY the raw JSON object.
Do NOT wrap the response in markdown code blocks.
Do NOT include ```json or ``` markers.
Do NOT add any explanatory text before or after the JSON.
The response must start with { and end with }.

-----------------

const item = $input.first().json; let parsedData; try { if (item.candidates && Array.isArray(item.candidates) && item.candidates[0]?.content?.parts?.[0]?.text) { const textContent = item.candidates[0].content.parts[0].text; const cleanedText = textContent.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim(); parsedData = JSON.parse(cleanedText); } else if (item.output) { const cleanedOutput = typeof item.output === 'string' ? item.output.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim() : item.output; parsedData = typeof cleanedOutput === 'string' ? JSON.parse(cleanedOutput) : cleanedOutput; } else if (item.results && Array.isArray(item.results)) { parsedData = item; } else { parsedData = null; } } catch (e) { console.error('JSON Parse Error:', e.message); parsedData = null; } let finalResults = []; if (parsedData && parsedData.results && Array.isArray(parsedData.results)) { finalResults = parsedData.results.map(r => ({ title: r.title || '', main_keyword: r.main_keyword || '', sub_keyword_1: r.sub_keyword_1 || '', sub_keyword_2: r.sub_keyword_2 || '' })); } if (finalResults.length === 0) { finalResults = [{ title: '추출 실패 (형식 확인 필요)', main_keyword: '패턴 불일치', sub_keyword_1: '', sub_keyword_2: '' }, { title: '추출 실패 (형식 확인 필요)', main_keyword: '패턴 불일치', sub_keyword_1: '', sub_keyword_2: '' }, { title: '추출 실패 (형식 확인 필요)', main_keyword: '패턴 불일치', sub_keyword_1: '', sub_keyword_2: '' }]; } return { json: { results: finalResults, target_store: $('Code in JavaScript1').first().json.target_store } };

-----------------

2. 블로그 글 작성

1에서 나온 제목과 대표키워드, 서브키워드1,2를 사용하여 블로그 글을 작성. 이것은 블로그의 품질이 중요하니 꼭 클로드 오푸스4.6을 사용해야 합니다.

키워드,글의주제,글의제목,직업명/상호명,메인키워드,서브키워드을 입력하면 내용이 나오게 하고 싶습니다.

2-1 자료 추출을 위해 퍼플렉시티 api를 연결하고 사용 했습니다.
로직
--------------
{{ { "model": "sonar", "messages": [{ "role": "user", "content": "키워드: " + $json["키워드"] + " 에 대해 조사하고 블로그 질문 5개 뽑아줘\n\n요구사항:\n1. 위 키워드와 직접적으로 관련된 핵심 정보, 개념, 트렌드를 요약해 주세요.\n2. 블로그 독자가 궁금해할 만한 검색용 질문 5개를 생성해 주세요.\n3. 각 질문은 실제로 검색에 사용할 수 있도록 구체적이고 명확한 문장으로 작성해 주세요.\n4. 질문은 서로 중복되지 않도록 다양한 관점(개념, 방법, 사례, 장단점, 최신 동향 등)을 반영해 주세요.\n5. 가능하다면 신뢰할 수 있는 출처 유형(블로그, 리서치, 공식 문서 등)을 함께 고려해 주세요.\n\n출력 형식:\n- [자료 요약]\n- [후속 검색 질문 5개 목록]" }] } }}

퍼플렉시티 검색시 밑에 후속 검색 질문이 나오는것을 다 확인하여 자료의 질을 올렸습니다.
------------

2-2 이후 클로드 오푸스4.6으로 글 작성
로직
--------------
[입력 정보]
글의 주제: {{ $('Google Sheets Trigger').item.json['글의주제'] }}\n글의 제목: {{ $('Google Sheets Trigger').item.json['글의제목'] }}\n직업명/상호명: {{ $('Google Sheets Trigger').item.json['직업명/상호명'] }}\n메인 키워드: {{ $('Google Sheets Trigger').item.json['메인키워드'] }}\n서브 키워드: {{ $('Google Sheets Trigger').item.json['서브키워드'] }}\n\n위 정보를 바탕으로 미리 입력한 시스템 가이드에 맞춰 블로그 포스팅을 완성해줘. 
-------------
// 현재 입력 데이터
const inputData = $input.item.json;

// ★★★ IF 노드를 통과한 시트 데이터 가져오기 ★★★
let sheetData = {};
try {
  sheetData = $('If').item.json;
} catch (e) {
  try {
    sheetData = $('Google Sheets Trigger').item.json;
  } catch (e2) {
    sheetData = {};
  }
}

// Claude 응답에서 본문 가져오기
let content = inputData.text || inputData.content || '';
if (typeof content !== 'string') {
  content = JSON.stringify(content) || '';
}

// 금지어 목록
const prohibitedWords = [
  '시술', '치료', '무절개', '최소절개', '무통증', '임플란트', '질환', '질병', 
  '의료', '예방', '내원', '의사', '의료기기', '병원', '수술', '성형', '의약품',
  '완치', '확실', '가장', '제일', '최신의', '최고의', '최상의', '최소한',
  '부작용없이', '안전한', '보장', '정확', '치료경험담', 
  '시술후기', '추천', '선정의료기관', '전문가',
  '최고', '최초', '최대', '최상', '강추', '1등', '정답', '100%', '0%',
  '할인', '무료', '공짜', '대박', '최저가', '상담'
];

const cautionPhrases = [
  '치료 효과', '100% 효과', '의학적으로 입증',
  '부작용 없는', '만병통치', '즉시효과'
];

// 검사 함수
function findOverusedWords(text) {
  if (!text || typeof text !== 'string') return [];
  try {
    const words = text.match(/[가-힣]+/g) || [];
    const wordCount = {};
    words.forEach(word => {
      if (word.length >= 2) {
        wordCount[word] = (wordCount[word] || 0) + 1;
      }
    });
    return Object.entries(wordCount)
      .filter(([word, count]) => count >= 20)
      .map(([word, count]) => word + '(' + count + '회)');
  } catch (e) {
    return [];
  }
}

// 검사 실행
const foundProhibited = prohibitedWords.filter(word => content.includes(word));
const foundCaution = cautionPhrases.filter(phrase => content.includes(phrase));
const overusedWords = findOverusedWords(content);

const needsRevision = foundProhibited.length > 0 || foundCaution.length > 0 || overusedWords.length > 0;

let revisionReasons = [];
if (foundProhibited.length > 0) revisionReasons.push('금지어: ' + foundProhibited.join(', '));
if (foundCaution.length > 0) revisionReasons.push('주의표현: ' + foundCaution.join(', '));
if (overusedWords.length > 0) revisionReasons.push('20회이상 반복: ' + overusedWords.join(', '));

// ★★★ 결과에 모든 시트 데이터 포함 ★★★
const result = {};

// 시트 원본 데이터 복사
Object.keys(sheetData).forEach(key => {
  result[key] = sheetData[key];
});

// 검사 결과 추가
result.content = content;
result.text = content;
result.needsRevision = needsRevision;
result.revisionReasons = revisionReasons.join(' | ') || '수정사항 없음';

return { json: result };

-----------------

2-3. 클로드 오푸스 4.6으로  내용을 검증하고 수정하는 과정을 한번더 거쳤습니다.

당신은 광고법을 준수하는 블로그 에디터입니다.
아래 글에서 발견된 문제를 수정해주세요.

[발견된 문제]
{{ $json.revisionReasons }}

[금지어 → 대체어 변환표]

의료/과장 표현:
- 가장/최고/최상/제일 → 돋보이는, 우수한, 뛰어난, 인기 있는
- 최초/유일 → 차별화된, 특별한
- 완벽/확실/정확 → 꼼꼼한, 세심한, 정밀한, 만족스러운
- 보장/약속 → 기대할 수 있는, 도움이 될 수 있는
- 100%/0% → 많은 분들이, 대부분
- 전문가 → 경험 많은 안경사, 숙련된 안경사, 베테랑 안경사
- 추천 → 안내, 소개, 제안
- 상담 → 문의, 방문, 확인, 이야기
- 효과/효능 → 도움, 변화, 장점
- 치료/시술 → 관리, 케어, 서비스
- 안전한 → 편안한, 부담 없는
- 즉시/바로 → 빠르게, 신속하게

마케팅 표현:
- 무료/공짜 → 추가 비용 없이, 서비스로 제공
- 최저가 → 합리적인 가격, 부담 없는 가격
- 할인 → 혜택, 특별 가격
- 이벤트 → 기회, 프로모션
- 강추/대박 → 만족도 높은, 호평받는

[수정 규칙]
1. 위 변환표를 참고해서 금지어를 자연스럽게 대체
2. 20회 이상 반복된 단어는 동의어로 분산 (예: 안경 → 아이웨어, 렌즈, 착용)
3. 글 길이 2,000자 내외 유지
4. 자연스러운 문장 흐름 유지
5. 숫자 나열(1. 2. 3.) 대신 문장으로 풀어서 작성

[원본 글]
{{ $json.content }}

위 규칙에 맞게 전체 글을 자연스럽게 다시 작성해주세요.
-------------------------
// Claude 응답
const claudeResponse = $input.item.json;

// 수정필요여부 IF 노드에서 시트 데이터 가져오기
let sheetData = {};
try {
  sheetData = $('수정필요여부').item.json;
} catch (e) {
  try {
    sheetData = $('검사_키워드_금지어').item.json;
  } catch (e2) {
    try {
      sheetData = $('If').item.json;
    } catch (e3) {
      sheetData = {};
    }
  }
}

// Claude 응답에서 텍스트 추출
let text = '';
if (claudeResponse.content && Array.isArray(claudeResponse.content)) {
  const textContent = claudeResponse.content.find(c => c.type === 'text');
  if (textContent && textContent.text) {
    text = textContent.text;
  }
} else {
  text = claudeResponse.text || claudeResponse.content || '';
}

// ★★★ 시트 데이터 + 새 텍스트 결합 ★★★
const result = {};

Object.keys(sheetData).forEach(key => {
  result[key] = sheetData[key];
});

result.text = text;
result.content = text;

return { json: result };
-----------------------
n8n 자동화 하기 전 내가 직접 썼던 프롬프트를 제공하겠습니다.

📋 안경원 전문성 강화형 블로그 프롬프트 (2000자/소주제 명확화/유동적 표 배치)

[역할 설정]
당신은 대학병원 검안팀 출신이자 동네에서 가장 친절한 **'이웃집 전문 안경사'**입니다.
단순히 제품을 파는 것이 아니라 **'광학적 지식'**과 **'임상 경험'**을 바탕으로 고객의 눈 건강 솔루션을 제공하는 전문가입니다.
어려운 광학 지식을 할머니도 이해할 수 있을 만큼 쉽고 다정하게 '번역(Washing)'해서 설명하며, 고객의 불편함에 깊이 공감합니다.

[입력 정보]

글의 주제: [입력]

글의 제목: [입력]

직업명/상호명: [입력]

메인 키워드: [입력]

서브 키워드: [입력]

[작성 지침]

1. 목표 분량 및 구조 (중요)

글자 수: 공백 포함 약 2000자 내외로 작성하세요. 짧은 호흡의 글이 아니라, 전문가로서의 식견을 충분히 담아내는 '상세한 정보성 글'이어야 합니다.

구조화: 긴 글이 지루하지 않도록 3~4개의 소제목으로 본문을 명확히 구분하세요.

소제목 스타일: 질문형이나 대화형(하나요?, 알아볼까요?)을 사용하지 마세요. 핵심 내용을 요약하는 **'명사형' 또는 '담백한 서술형'**으로 작성하세요. (예: 블루라이트 차단 원리, 누진다초점 렌즈 적응 노하우)

문단 끊기: 모바일 가독성을 위해 한 문단은 최대 3~4줄을 넘기지 말고 줄바꿈(엔터)을 자주 사용하세요.

2. 문체 및 톤앤매너 (Friendly Body, Professional Header)

본문(내용): "~입니다/합니다" 같은 딱딱한 문어체 대신 "~해요" "~하거든요" "~한답니다" "~까요?" 등 옆에서 말하듯 100% 구어체를 사용하세요.

소제목(헤더): 본문과 달리 전문적이고 신뢰감 있는 딱딱한 톤을 유지하세요.

쉼표(,) 절대 금지: 문장의 호흡을 쉼표로 끊지 말고, 적절한 접속사('그런데' '사실은' '그래서' '왜냐하면')와 연결 어미('~해서' '~하니까' '~한데')를 사용하여 물 흐르듯 이어지게 하세요.

전문용어 번역: 어려운 광학 용어는 반드시 일상적인 비유(Analogy)를 들어 쉽게 풀어주세요.

3. 본문 작성 공식 (Logic & Detail)
분량을 억지로 늘리지 말고, 다음 **[전문가 설명 3단계]**를 적용해 밀도 있게 작성하세요.

1단계 (원리): 왜 눈이 불편한지, 렌즈가 왜 필요한지 광학적/해부학적 원리를 깊이 있게 설명 (전문성).

2단계 (풀이): 그 원리를 우리 일상생활이나 쉬운 사물에 빗대어 설명 (이해).

3단계 (혜택): 그래서 이 솔루션을 선택했을 때 고객의 삶이 어떻게 편해지는지 구체적 묘사 (설득).

4. 시각적 요소 (표 배치)

표(Markdown Table) 필수: 2000자의 긴 호흡 중간에 환기구가 필요합니다.

위치: 특정 위치에 강제하지 말고, 정보 비교(장단점, 가격, 스펙 등)나 요약이 필요한 가장 적절한 타이밍에 1~2개 삽입하세요.

볼드체 강조: 핵심 키워드나 중요한 문구는 볼드체로 처리하여 스캐닝이 가능하게 하세요.

5. 금지 사항 및 의료법 필터링 (Strict Rules)
아래 내용은 절대 포함하지 마세요. (블로그 품질 및 법적 리스크 관리)

금지 단어(과장/단정): "최고" "최상" "유일" "100% 완치" "무조건 해결" "즉시 효과" "전혀 없음" "기적의" 등.

금지 표현(비교/비방): "다른 안경원보다 낫다" "타 제품은 별로다" 식의 비교 우월 표현.

대체 표현 사용: "해결됩니다" -> "도움을 줄 수 있어요", "없어집니다" -> "개선될 수 있답니다"로 순화.

형식 금지: 쉼표(,) 사용 금지, 이모지(😊) 사용 금지, "첫째, 둘째" 식의 기계적 나열 금지.

[출력 형식]

제목: [정보 기대감을 주는 자연스러운 제목 (20자 내외)]

[도입부]
(고객의 상황에 깊이 공감하고 문제를 인식시키는 부드러운 스몰토크 / 쉼표 없이 작성 / 상호명 노출 자제)

[본문]

(소주제 1 - 명사형/간결한 서술형)
(내용: 원리-풀이-혜택 구조로 상세히 서술 / 문단은 3~4줄씩 끊어서 작성)

(소주제 2 - 명사형/간결한 서술형)
(내용: 전문 지식을 아주 쉽게 비유하여 깊이 있게 설명 / 볼드체 활용)

(표는 내용 흐름상 비교나 요약이 필요한 가장 적절한 곳에 배치해주세요)
| 구분 | 내용 | 특징 |
| :--- | :--- | :--- |
| (항목) | (설명) | (상세) |

(소주제 3 - 명사형/간결한 서술형)
(내용: 구체적인 해결책 제시 / 의료법 준수 표현 사용 / "~할 수 있어요" 화법)

(소주제 4 - 명사형/간결한 서술형)
(내용: 관리 방법이나 안경사가 주는 꿀팁)

[결론]
(오늘 내용의 핵심 요약 및 안경사의 따뜻하고 담백한 조언 / 강요하지 않는 태도)

[매장 안내]
(상호명 및 편안한 마음으로 방문하시라는 권유 / 지도 정보가 들어갈 자리임을 표시)

---------------------------

3. 블로그 이미지생성
나노바나나2 프로로 이미지를 생성했습니다.
 api 를 넣고 돌릴수 있게 하여 html을 생성해서 사용했습니다.

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>나노바나나 프로 이미지 생성기 V5</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Malgun Gothic', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 1400px; margin: 0 auto; }
        header {
            background: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            margin-bottom: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        h1 { color: #333; font-size: 2rem; margin-bottom: 10px; }
        .subtitle { color: #666; font-size: 0.95rem; }
        .card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        .form-group { margin-bottom: 20px; }
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        input, textarea, select {
            width: 100%;
            padding: 12px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 1rem;
            font-family: inherit;
        }
        textarea { min-height: 300px; resize: vertical; }
        input:focus, textarea:focus, select:focus {
            outline: none;
            border-color: #667eea;
        }
        .hint { font-size: 0.85rem; color: #666; margin-top: 5px; }
        button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: transform 0.2s;
        }
        button:hover:not(:disabled) { transform: translateY(-2px); }
        button:disabled { background: #ccc; cursor: not-allowed; }
        .btn-small {
            padding: 8px 12px;
            font-size: 0.8rem;
            width: auto;
            display: inline-block;
        }
        .loading { text-align: center; padding: 40px; }
        .spinner {
            border: 4px solid #f3f3f3;
            border-top: 4px solid #667eea;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .hidden { display: none; }
        .progress-bar {
            width: 100%;
            height: 30px;
            background: #e0e0e0;
            border-radius: 15px;
            overflow: hidden;
            margin: 15px 0;
        }
        .progress-fill {
            height: 100%;
            background: linear-gradient(135deg, #4caf50 0%, #388e3c 100%);
            width: 0%;
            transition: width 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
        }
        .image-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        .image-item {
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            background: white;
            transition: transform 0.2s;
        }
        .image-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.2);
        }
        .image-item img {
            width: 100%;
            height: 280px;
            object-fit: cover;
            display: block;
        }
        .image-placeholder {
            width: 100%;
            height: 280px;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #999;
            font-size: 0.9rem;
        }
        .image-error { background: #ffebee; color: #c62828; }
        .image-info { padding: 15px; background: #f8f9fa; }
        .image-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }
        .image-number { font-weight: 600; color: #667eea; }
        .image-section {
            background: #e8f5e9;
            color: #2e7d32;
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 0.7rem;
        }
        .image-desc {
            font-size: 0.8rem;
            color: #666;
            margin-bottom: 12px;
            line-height: 1.4;
            max-height: 50px;
            overflow: hidden;
        }
        .image-actions {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
        }
        .image-actions button {
            flex: 1;
            min-width: 70px;
            padding: 8px 10px;
            font-size: 0.75rem;
        }
        .btn-download { background: linear-gradient(135deg, #4caf50 0%, #388e3c 100%); }
        .btn-edit { background: linear-gradient(135deg, #ff9800 0%, #f57c00 100%); }
        .btn-retry { background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%); }
        .btn-regenerate { background: linear-gradient(135deg, #9c27b0 0%, #7b1fa2 100%); }
        .info-box {
            background: #e3f2fd;
            border: 2px solid #2196f3;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
        }
        .file-upload-area {
            border: 2px dashed #667eea;
            border-radius: 8px;
            padding: 30px;
            text-align: center;
            background: #f8f9ff;
            cursor: pointer;
            transition: all 0.3s;
        }
        .file-upload-area:hover { background: #f0f2ff; border-color: #5568d3; }
        .file-upload-area.dragover { background: #e8ebff; border-color: #4450b5; }
        .file-preview {
            margin-top: 15px;
            padding: 15px;
            background: #e8f5e9;
            border-radius: 8px;
            display: none;
        }
        .file-preview.active { display: block; }
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.7);
        }
        .modal.active {
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .modal-content {
            background: white;
            padding: 30px;
            border-radius: 15px;
            max-width: 700px;
            width: 90%;
            max-height: 80vh;
            overflow-y: auto;
        }
        .modal-content h3 { margin-bottom: 20px; color: #333; }
        .modal-content textarea { min-height: 120px; }
        .modal-actions { display: flex; gap: 10px; margin-top: 20px; }
        .modal-actions button { flex: 1; }
        .log-box {
            background: #263238;
            color: #aed581;
            padding: 15px;
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-size: 0.8rem;
            max-height: 150px;
            overflow-y: auto;
            margin-top: 15px;
        }
        .log-success { color: #81c784; }
        .log-error { color: #e57373; }
        .log-info { color: #64b5f6; }
        .section-list {
            background: #fff8e1;
            border: 1px solid #ffca28;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
        }
        .section-list h4 {
            color: #f57f17;
            margin-bottom: 10px;
            font-size: 0.9rem;
        }
        .section-tag {
            display: inline-block;
            background: white;
            border: 1px solid #ffc107;
            color: #ff8f00;
            padding: 4px 10px;
            border-radius: 15px;
            font-size: 0.75rem;
            margin: 3px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🎨 나노바나나 프로 이미지 생성기</h1>
            <p class="subtitle">원고 소제목/흐름에 맞는 이미지 자동 생성</p>
        </header>

        <div class="card" id="apiSetup">
            <h3 style="margin-bottom: 15px;">🔑 Google AI Studio API 키</h3>
            <div class="form-group">
                <input type="password" id="apiKey" placeholder="AIza...">
                <p class="hint">발급: <a href="https://aistudio.google.com/apikey" target="_blank" style="color: #1976d2;">aistudio.google.com/apikey</a></p>
            </div>
            <button onclick="saveApiKey()">✅ 시작하기</button>
        </div>

        <div class="card hidden" id="mainForm">
            <h3 style="margin-bottom: 20px;">📝 블로그 원고 입력</h3>
            
            <div class="form-group">
                <label>📎 원고 파일 업로드 (선택)</label>
                <div class="file-upload-area" id="fileUploadArea" onclick="document.getElementById('fileInput').click()">
                    <div style="font-size: 3rem; margin-bottom: 10px;">📄</div>
                    <div style="font-size: 1.1rem; font-weight: 600;">클릭하거나 드래그</div>
                </div>
                <input type="file" id="fileInput" accept=".txt" style="display: none;" onchange="handleFileSelect(event)">
                <div class="file-preview" id="filePreview">
                    <span style="font-weight: 600;">✅ <span id="fileName"></span></span>
                    <button class="btn-small" onclick="clearFile()" style="background: #f44336; margin-left: 10px;">삭제</button>
                </div>
            </div>
            
            <div class="form-group">
                <label>블로그 원고 *</label>
                <textarea id="manuscript" placeholder="블로그 원고를 붙여넣으세요...

※ 소제목이 있으면 더 정확한 이미지가 생성됩니다.
예시:
# 콘택트렌즈 올바른 착용법

## 1. 손 씻기의 중요성
렌즈를 만지기 전에 반드시 손을 깨끗이...

## 2. 렌즈 착용 방법
검지 손가락 위에 렌즈를 올려놓고..."></textarea>
            </div>

            <div id="sectionPreview" class="section-list hidden">
                <h4>📋 감지된 소제목 (이 내용에 맞는 이미지 생성)</h4>
                <div id="sectionTags"></div>
            </div>

            <div class="info-box">
                <p style="color: #1565c0;"><strong>📅 현재:</strong> <span id="currentSeason"></span></p>
                <p style="color: #1565c0; margin-top: 5px;"><strong>✨ 원고의 소제목/핵심 내용에 맞는 이미지를 생성합니다</strong></p>
            </div>

            <button onclick="generateImages()" id="generateBtn">🚀 이미지 10장 생성</button>
        </div>

        <div id="loadingSection" class="card hidden">
            <div class="loading">
                <div class="spinner"></div>
                <p id="statusText" style="font-size: 1.1rem; color: #667eea; font-weight: 600;"></p>
                <div class="progress-bar">
                    <div class="progress-fill" id="progressBar">0%</div>
                </div>
                <div class="log-box" id="logBox"></div>
            </div>
        </div>

        <div id="imageSection" class="card hidden">
            <h2>🖼️ 생성된 이미지</h2>
            <p style="margin: 10px 0; color: #666;"><span id="successCount">0</span>장 성공 / <span id="failCount">0</span>장 실패</p>
            <div class="image-grid" id="imageGrid"></div>
        </div>
    </div>

    <div id="editModal" class="modal">
        <div class="modal-content">
            <h3>✏️ 프롬프트 수정 후 재생성</h3>
            <div class="form-group">
                <label>현재 섹션</label>
                <input type="text" id="editSection" readonly style="background: #f5f5f5;">
            </div>
            <div class="form-group">
                <label>프롬프트 (수정 가능)</label>
                <textarea id="editPrompt"></textarea>
            </div>
            <div class="modal-actions">
                <button onclick="closeModal()" style="background: #9e9e9e;">취소</button>
                <button onclick="applyEditAndRegenerate()">🔄 수정 후 재생성</button>
            </div>
        </div>
    </div>

    <script type="module">
        import { GoogleGenerativeAI } from "https://esm.run/@google/generative-ai";

        let genAI;
        let apiKey = localStorage.getItem('nanoApiKey') || '';
        let currentPrompts = [];
        let currentSections = [];
        let currentEditIndex = -1;
        let imageResults = [];

        window.onload = function() {
            if (apiKey) document.getElementById('apiKey').value = apiKey;
            setupDragDrop();
            displaySeason();
            
            // 원고 입력 시 소제목 미리보기
            document.getElementById('manuscript').addEventListener('input', previewSections);
        };

        function previewSections() {
            const text = document.getElementById('manuscript').value;
            const sections = extractSections(text);
            
            const preview = document.getElementById('sectionPreview');
            const tags = document.getElementById('sectionTags');
            
            if (sections.length > 0) {
                tags.innerHTML = sections.map(s => `<span class="section-tag">${s}</span>`).join('');
                preview.classList.remove('hidden');
            } else {
                preview.classList.add('hidden');
            }
        }

        function extractSections(text) {
            const sections = [];
            const lines = text.split('\n');
            
            for (let line of lines) {
                line = line.trim();
                // # 또는 ## 으로 시작하는 제목
                if (line.match(/^#{1,3}\s+(.+)/)) {
                    const title = line.replace(/^#{1,3}\s+/, '').trim();
                    if (title.length > 0 && title.length < 50) {
                        sections.push(title);
                    }
                }
                // 숫자. 으로 시작하는 소제목
                else if (line.match(/^\d+[\.\)]\s*(.+)/)) {
                    const title = line.replace(/^\d+[\.\)]\s*/, '').trim();
                    if (title.length > 2 && title.length < 50) {
                        sections.push(title);
                    }
                }
                // **굵은 글씨** 소제목
                else if (line.match(/^\*\*(.+)\*\*$/)) {
                    const title = line.replace(/\*\*/g, '').trim();
                    if (title.length > 2 && title.length < 50) {
                        sections.push(title);
                    }
                }
            }
            
            return sections.slice(0, 10);
        }

        function addLog(msg, type = 'info') {
            const box = document.getElementById('logBox');
            const div = document.createElement('div');
            div.className = `log-${type}`;
            div.textContent = `[${new Date().toLocaleTimeString()}] ${msg}`;
            box.appendChild(div);
            box.scrollTop = box.scrollHeight;
        }

        function displaySeason() {
            const month = new Date().getMonth() + 1;
            const year = new Date().getFullYear();
            let season;
            if (month >= 3 && month <= 5) season = "🌸 봄";
            else if (month >= 6 && month <= 8) season = "☀️ 여름";
            else if (month >= 9 && month <= 11) season = "🍂 가을";
            else season = "❄️ 겨울";
            document.getElementById('currentSeason').textContent = `${year}년 ${month}월 (${season})`;
        }

        function setupDragDrop() {
            const area = document.getElementById('fileUploadArea');
            area.addEventListener('dragover', (e) => { e.preventDefault(); area.classList.add('dragover'); });
            area.addEventListener('dragleave', () => area.classList.remove('dragover'));
            area.addEventListener('drop', (e) => {
                e.preventDefault();
                area.classList.remove('dragover');
                if (e.dataTransfer.files[0]) handleFile(e.dataTransfer.files[0]);
            });
        }

        window.handleFileSelect = (e) => { if (e.target.files[0]) handleFile(e.target.files[0]); };

        function handleFile(file) {
            document.getElementById('fileName').textContent = file.name;
            document.getElementById('filePreview').classList.add('active');
            const reader = new FileReader();
            reader.onload = (e) => {
                document.getElementById('manuscript').value = e.target.result;
                previewSections();
            };
            reader.readAsText(file);
        }

        window.clearFile = () => {
            document.getElementById('fileInput').value = '';
            document.getElementById('filePreview').classList.remove('active');
        };

        window.saveApiKey = function() {
            apiKey = document.getElementById('apiKey').value.trim();
            if (!apiKey || !apiKey.startsWith('AIza')) {
                alert('올바른 API 키를 입력해주세요!');
                return;
            }
            localStorage.setItem('nanoApiKey', apiKey);
            genAI = new GoogleGenerativeAI(apiKey);
            document.getElementById('apiSetup').classList.add('hidden');
            document.getElementById('mainForm').classList.remove('hidden');
            alert('✅ API 키 저장 완료!');
        };

        function updateProgress(percent, text) {
            document.getElementById('progressBar').style.width = percent + '%';
            document.getElementById('progressBar').textContent = percent + '%';
            document.getElementById('statusText').textContent = text;
        }

        window.generateImages = async function() {
            const manuscript = document.getElementById('manuscript').value.trim();
            if (!manuscript) {
                alert('원고를 입력해주세요!');
                return;
            }
            if (!genAI) genAI = new GoogleGenerativeAI(apiKey);

            document.getElementById('logBox').innerHTML = '';
            document.getElementById('loadingSection').classList.remove('hidden');
            document.getElementById('imageSection').classList.add('hidden');
            document.getElementById('generateBtn').disabled = true;
            imageResults = [];

            try {
                // 소제목 추출
                const sections = extractSections(manuscript);
                addLog(`소제목 ${sections.length}개 감지: ${sections.join(', ')}`, 'info');
                
                updateProgress(5, '원고 분석 중...');
                
                const result = await generatePrompts(manuscript, sections);
                currentPrompts = result.prompts;
                currentSections = result.sections;
                
                addLog(`프롬프트 ${currentPrompts.length}개 생성`, 'success');
                
                updateProgress(15, '이미지 생성 시작...');
                await createImages(currentPrompts);
                
                updateProgress(100, '완료!');
                addLog('완료!', 'success');
                
                setTimeout(() => {
                    document.getElementById('loadingSection').classList.add('hidden');
                }, 1000);
                
            } catch (error) {
                addLog(`에러: ${error.message}`, 'error');
                alert('에러: ' + error.message);
            } finally {
                document.getElementById('generateBtn').disabled = false;
            }
        };

        async function generatePrompts(manuscript, sections) {
            const now = new Date();
            const month = now.getMonth() + 1;
            const year = now.getFullYear();
            
            // 계절 자동 감지
            let season, seasonKr, clothing, lighting;
            
            if (month >= 3 && month <= 5) {
                season = "Spring"; seasonKr = "봄";
                clothing = "light trench coat, pastel cardigan, cotton blouse";
                lighting = "bright spring daylight";
            } else if (month >= 6 && month <= 8) {
                season = "Summer"; seasonKr = "여름";
                clothing = "short sleeves, linen shirt, light summer dress";
                lighting = "bright natural summer light";
            } else if (month >= 9 && month <= 11) {
                season = "Autumn"; seasonKr = "가을";
                clothing = "knit sweater, light wool coat, earth-tone layers";
                lighting = "warm golden hour light";
            } else {
                season = "Winter"; seasonKr = "겨울";
                clothing = "thick wool coat, warm knitwear, turtleneck, scarf";
                lighting = "warm indoor lighting, soft cozy glow";
            }

            // 소제목 가이드
            const sectionGuide = sections.length > 0 
                ? `\n\n## 원고의 주요 소제목:
${sections.map((s, i) => `- "${s}"`).join('\n')}`
                : '';

            const systemPrompt = `# 역할
당신은 안경원 전문 상업 사진 포토그래퍼이자 프롬프트 엔지니어입니다.

# 핵심 원칙 (가장 중요!)
★★★ 원고의 구체적인 내용을 그대로 이미지로 표현해야 합니다 ★★★

원고에 없는 내용은 절대 넣지 마세요:
- 원고에 "콘택트렌즈"가 없으면 콘택트렌즈 이미지 금지
- 원고에 "어린이"가 없으면 어린이 이미지 금지
- 원고에 "노안/다초점"이 없으면 중장년 이미지 금지

# 원고 분석 방법

## 1단계: 핵심 키워드 추출
원고에서 다음을 찾으세요:
- 주인공 (나이, 직업, 상황)
- 문제/증상 (어지럼증, 두통, 피로 등)
- 원인 (축 오차, 도수 문제 등)
- 해결책 (검사 방법, 교정 등)
- 비유/예시 (원고에 나온 비유 그대로 사용)

## 2단계: 원고 내용 → 이미지 변환
원고의 각 단락/문장을 이미지로 변환하세요:
- "컴퓨터 화면 볼 때 어지럽다" → 컴퓨터 앞에서 어지러워하는 장면
- "축이 15도 어긋남" → 검안 장면, 축 측정 장면
- "럭비공처럼 찌그러진" → 럭비공 모양 비유 이미지
- "카메라 렌즈에 빗방울" → 카메라 렌즈 물방울 비유 이미지
- "수동 검영법" → 수동 검영 검사 장면

# 오늘 날짜: ${year}년 ${month}월 (${seasonKr})
# 계절 의상: ${clothing}
# 조명: ${lighting}
${sectionGuide}

# 이미지 배분
- 인물 이미지 5개 (원고 주인공/상황에 맞는 한국인)
- 정보 이미지 5개 (원고에 나온 개념/도구/비유)

# 출력 형식

NO. [번호]
[장면 요약]: (원고의 어느 부분을 표현하는지 구체적으로)
[이미지 유형]: (인물 또는 정보)
[Positive Prompt]: (영어 프롬프트)
[한글 번역]: (의도 설명)

# 프롬프트 규칙

## 인물 이미지
- 반드시 "Korean" 명시
- 원고에 나온 연령대/직업/상황 그대로 반영
- 원고의 구체적 행동/증상 묘사

## 정보 이미지  
- 원고에 나온 개념/비유/도구를 직접 표현
- "no text, no letters, no words" 필수 포함
- 원고에 없는 제품은 넣지 않기

# 블로그 원고
${manuscript}

★ 위 원고를 정밀 분석하여 원고 내용에 100% 부합하는 10개 프롬프트를 생성하세요.
★ 원고에 없는 내용(콘택트렌즈, 어린이 등)은 절대 포함하지 마세요.
★ 원고의 비유, 예시, 구체적 상황을 그대로 이미지로 표현하세요.`;

            try {
                const model = genAI.getGenerativeModel({ model: "gemini-2.0-flash" });
                addLog('Gemini API 호출 중... (원고 밀착 분석)', 'info');
                
                const result = await model.generateContent(systemPrompt);
                const text = result.response.text();
                console.log("Generated prompts:", text);
                
                const prompts = [];
                const promptSections = [];
                
                // [Positive Prompt]: 추출
                const positiveMatches = text.matchAll(/\[Positive Prompt\]:\s*(.+?)(?=\[한글 번역\]|\[Negative|\n\n|NO\.\s*\d|$)/gis);
                for (const match of positiveMatches) {
                    let prompt = match[1].trim();
                    prompt = prompt.replace(/\n/g, ' ').replace(/\s+/g, ' ').trim();
                    prompt = prompt.replace(/^\(/, '').replace(/\)$/, '');
                    if (!prompt.includes('--ar')) prompt += ' --ar 4:3';
                    if (prompt.length > 30) {
                        prompts.push(prompt);
                    }
                }
                
                // [장면 요약]에서 섹션명 추출
                const sectionMatches = text.matchAll(/\[장면 요약\]:\s*(.+?)(?=\[이미지|\[설정|\n)/gi);
                for (const match of sectionMatches) {
                    promptSections.push(match[1].trim().substring(0, 50));
                }
                
                addLog(`Positive Prompt ${prompts.length}개 추출`, 'info');
                
                // 부족하면 대체 파싱
                if (prompts.length < 5) {
                    addLog('대체 파싱 시도...', 'info');
                    const lines = text.split('\n');
                    for (let line of lines) {
                        const lower = line.toLowerCase();
                        if ((lower.includes('korean') || 
                             lower.includes('close-up') || 
                             lower.includes('lens') || 
                             lower.includes('frame') ||
                             lower.includes('glasses') ||
                             lower.includes('optical') ||
                             lower.includes('phoropter') ||
                             lower.includes('computer') ||
                             lower.includes('dslr')) && 
                            line.length > 50 && 
                            !line.includes('[장면') && 
                            !line.includes('[이미지')) {
                            let prompt = line.replace(/^\[Positive Prompt\]:\s*/i, '').trim();
                            prompt = prompt.replace(/^\(/, '').replace(/\)$/, '');
                            prompt = prompt.replace(/^[-\d\.\)]+\s*/, '');
                            if (!prompt.includes('--ar')) prompt += ' --ar 4:3';
                            if (!prompts.includes(prompt) && prompt.length > 30) {
                                prompts.push(prompt);
                            }
                        }
                    }
                }
                
                // 10개 미만이면 원고 기반 기본값 (일반적인 안경원 장면)
                const defaultScenes = [
                    { section: '고객 증상', prompt: `Korean office worker in their 30s wearing ${clothing}, feeling dizzy while looking at computer screen, rubbing temples with tired expression, modern office setting, ${lighting}, real photo, DSLR 8k --ar 4:3` },
                    { section: '안경 검사', prompt: `Korean optician examining Korean customer's glasses with professional tools, checking lens alignment, modern optical shop, soft lighting, real photo, DSLR 8k --ar 4:3` },
                    { section: '시력 검사', prompt: `Korean customer in their 30s looking through manual phoropter with analog dials, Korean optician adjusting equipment carefully, clean examination room, professional lighting, real photo, DSLR 8k --ar 4:3` },
                    { section: '상담 설명', prompt: `Korean optician explaining lens diagram to Korean customer at consultation table, pointing at visual chart, modern optical shop, warm lighting, real photo, DSLR 8k --ar 4:3` },
                    { section: '교정 후 만족', prompt: `Korean office worker in their 30s wearing new glasses, looking at computer screen comfortably with relieved smile, modern office, ${lighting}, real photo, DSLR 8k --ar 4:3` },
                    { section: '렌즈 검사 장비', prompt: `Manual phoropter with analog adjustment dials in optometry room, professional optical equipment detail, clinical lighting, no text, no letters, no words, DSLR 8k --ar 4:3` },
                    { section: '렌즈 클로즈업', prompt: `Close-up of eyeglass lens showing light refraction and astigmatism correction, studio lighting, clean background, no text, no letters, product photography, DSLR 8k --ar 4:3` },
                    { section: '검안 도구', prompt: `Professional optometry tools including trial lens set and lens meter on clean surface, optical equipment arrangement, soft studio lighting, no text, no words, DSLR 8k --ar 4:3` },
                    { section: '시력표', prompt: `Eye examination equipment with trial frames in modern optometry room, professional medical setting, clean clinical lighting, no text, no letters, DSLR 8k --ar 4:3` },
                    { section: '안경 조정', prompt: `Close-up of optician's hands adjusting eyeglass frame alignment with precision tools, professional optical work, soft focused lighting, no text, DSLR 8k --ar 4:3` }
                ];
                
                let defaultIdx = 0;
                while (prompts.length < 10) {
                    const def = defaultScenes[defaultIdx % defaultScenes.length];
                    prompts.push(def.prompt);
                    promptSections.push(def.section);
                    defaultIdx++;
                }
                
                while (promptSections.length < 10) {
                    promptSections.push('원고 관련');
                }
                
                return {
                    prompts: prompts.slice(0, 10),
                    sections: promptSections.slice(0, 10)
                };
                
            } catch (error) {
                console.error('Prompt generation error:', error);
                addLog(`프롬프트 생성 에러: ${error.message}`, 'error');
                throw error;
            }
        }

        async function createImages(prompts) {
            const grid = document.getElementById('imageGrid');
            grid.innerHTML = '';
            document.getElementById('imageSection').classList.remove('hidden');
            
            let success = 0, fail = 0;
            
            for (let i = 0; i < prompts.length; i++) {
                addLog(`이미지 ${i + 1}/10 생성 중... (${currentSections[i]})`, 'info');
                updateProgress(15 + (i + 1) * 8, `이미지 ${i + 1}/10 생성 중...`);
                
                addPlaceholder(i);
                
                try {
                    const data = await generateImage(prompts[i], i);
                    if (data) {
                        updateImage(i, data, prompts[i], true);
                        imageResults[i] = { success: true, data, prompt: prompts[i] };
                        success++;
                        addLog(`이미지 ${i + 1} 성공 ✓`, 'success');
                    } else {
                        updateImage(i, null, prompts[i], false);
                        imageResults[i] = { success: false, prompt: prompts[i] };
                        fail++;
                        addLog(`이미지 ${i + 1} 실패`, 'error');
                    }
                } catch (error) {
                    updateImage(i, null, prompts[i], false);
                    imageResults[i] = { success: false, prompt: prompts[i] };
                    fail++;
                    addLog(`이미지 ${i + 1} 에러: ${error.message}`, 'error');
                }
                
                document.getElementById('successCount').textContent = success;
                document.getElementById('failCount').textContent = fail;
                
                if (i < prompts.length - 1) await new Promise(r => setTimeout(r, 1500));
            }
        }

        async function generateImage(prompt, index) {
            // 나노바나나 프로 - 원본과 동일
            const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-3-pro-image-preview:generateContent?key=${apiKey}`;
            
            const body = {
                contents: [{ parts: [{ text: prompt }] }],
                generationConfig: {
                    responseModalities: ['IMAGE'],
                    imageConfig: { aspectRatio: '4:3' }
                }
            };

            try {
                const res = await fetch(url, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(body)
                });

                if (!res.ok) {
                    const err = await res.text();
                    console.error(`API Error ${index + 1}:`, err);
                    return null;
                }

                const data = await res.json();
                if (data.candidates?.[0]?.content?.parts) {
                    for (let part of data.candidates[0].content.parts) {
                        if (part.inlineData?.data) return part.inlineData.data;
                    }
                }
                return null;
            } catch (e) {
                console.error(`Fetch error ${index + 1}:`, e);
                throw e;
            }
        }

        function addPlaceholder(i) {
            const grid = document.getElementById('imageGrid');
            const div = document.createElement('div');
            div.className = 'image-item';
            div.id = `img-container-${i}`;
            div.innerHTML = `
                <div class="image-placeholder">
                    <div style="text-align: center;">
                        <div class="spinner" style="width: 30px; height: 30px; border-width: 3px;"></div>
                        <p style="margin-top: 10px;">이미지 ${i + 1} 생성 중...</p>
                    </div>
                </div>
                <div class="image-info">
                    <div class="image-header">
                        <span class="image-number">${i + 1}/10</span>
                        <span class="image-section">${currentSections[i] || '생성중'}</span>
                    </div>
                    <div class="image-desc">생성 중...</div>
                </div>
            `;
            grid.appendChild(div);
        }

        function updateImage(i, data, prompt, success) {
            const container = document.getElementById(`img-container-${i}`);
            if (!container) return;
            
            const section = currentSections[i] || '일반';
            
            if (success && data) {
                container.innerHTML = `
                    <img src="data:image/png;base64,${data}" alt="이미지 ${i + 1}" id="img-${i}">
                    <div class="image-info">
                        <div class="image-header">
                            <span class="image-number">${i + 1}/10 ✅</span>
                            <span class="image-section">${section}</span>
                        </div>
                        <div class="image-desc">${prompt.substring(0, 80)}...</div>
                        <div class="image-actions">
                            <button class="btn-small btn-regenerate" onclick="regenerateSame(${i})">🔄 재생성</button>
                            <button class="btn-small btn-edit" onclick="editImage(${i})">✏️ 수정</button>
                            <button class="btn-small btn-download" onclick="downloadImage(${i})">⬇️ 저장</button>
                        </div>
                    </div>
                `;
            } else {
                container.innerHTML = `
                    <div class="image-placeholder image-error">
                        <div style="text-align: center;">
                            <p style="font-size: 2rem;">❌</p>
                            <p>생성 실패</p>
                        </div>
                    </div>
                    <div class="image-info">
                        <div class="image-header">
                            <span class="image-number">${i + 1}/10</span>
                            <span class="image-section">${section}</span>
                        </div>
                        <div class="image-desc">${prompt.substring(0, 60)}...</div>
                        <div class="image-actions">
                            <button class="btn-small btn-regenerate" onclick="regenerateSame(${i})">🔄 재생성</button>
                            <button class="btn-small btn-edit" onclick="editImage(${i})">✏️ 수정</button>
                        </div>
                    </div>
                `;
            }
        }

        // 같은 프롬프트로 재생성
        window.regenerateSame = async function(i) {
            const container = document.getElementById(`img-container-${i}`);
            const imgEl = container.querySelector('img, .image-placeholder');
            if (imgEl) {
                imgEl.outerHTML = `
                    <div class="image-placeholder">
                        <div style="text-align: center;">
                            <div class="spinner" style="width: 30px; height: 30px; border-width: 3px;"></div>
                            <p style="margin-top: 10px;">재생성 중...</p>
                        </div>
                    </div>
                `;
            }
            
            try {
                const data = await generateImage(currentPrompts[i], i);
                if (data) {
                    updateImage(i, data, currentPrompts[i], true);
                    imageResults[i] = { success: true, data, prompt: currentPrompts[i] };
                    updateCounts();
                    addLog(`이미지 ${i + 1} 재생성 성공`, 'success');
                } else {
                    updateImage(i, null, currentPrompts[i], false);
                    addLog(`이미지 ${i + 1} 재생성 실패`, 'error');
                }
            } catch (e) {
                updateImage(i, null, currentPrompts[i], false);
                addLog(`이미지 ${i + 1} 재생성 에러: ${e.message}`, 'error');
            }
        };

        function updateCounts() {
            let s = 0, f = 0;
            imageResults.forEach(r => r?.success ? s++ : f++);
            document.getElementById('successCount').textContent = s;
            document.getElementById('failCount').textContent = f;
        }

        // 프롬프트 수정 모달
        window.editImage = function(i) {
            currentEditIndex = i;
            document.getElementById('editSection').value = currentSections[i] || '일반';
            document.getElementById('editPrompt').value = currentPrompts[i];
            document.getElementById('editModal').classList.add('active');
        };

        window.closeModal = () => {
            document.getElementById('editModal').classList.remove('active');
            currentEditIndex = -1;
        };

        // 수정된 프롬프트로 재생성 (버그 수정)
        window.applyEditAndRegenerate = async function() {
            if (currentEditIndex === -1) return;
            
            const newPrompt = document.getElementById('editPrompt').value.trim();
            if (!newPrompt) {
                alert('프롬프트를 입력하세요!');
                return;
            }
            
            // 프롬프트 업데이트
            currentPrompts[currentEditIndex] = newPrompt;
            
            closeModal();
            
            // 수정된 프롬프트로 재생성
            const i = currentEditIndex;
            const container = document.getElementById(`img-container-${i}`);
            const imgEl = container.querySelector('img, .image-placeholder');
            if (imgEl) {
                imgEl.outerHTML = `
                    <div class="image-placeholder">
                        <div style="text-align: center;">
                            <div class="spinner" style="width: 30px; height: 30px; border-width: 3px;"></div>
                            <p style="margin-top: 10px;">수정된 프롬프트로 생성 중...</p>
                        </div>
                    </div>
                `;
            }
            
            try {
                addLog(`이미지 ${i + 1} 수정된 프롬프트로 재생성 중...`, 'info');
                const data = await generateImage(newPrompt, i);
                if (data) {
                    updateImage(i, data, newPrompt, true);
                    imageResults[i] = { success: true, data, prompt: newPrompt };
                    updateCounts();
                    addLog(`이미지 ${i + 1} 수정 재생성 성공 ✓`, 'success');
                } else {
                    updateImage(i, null, newPrompt, false);
                    addLog(`이미지 ${i + 1} 수정 재생성 실패`, 'error');
                }
            } catch (e) {
                updateImage(i, null, newPrompt, false);
                addLog(`이미지 ${i + 1} 수정 재생성 에러: ${e.message}`, 'error');
            }
        };

        window.downloadImage = function(i) {
            const img = document.getElementById(`img-${i}`);
            if (!img) return;
            const a = document.createElement('a');
            a.href = img.src;
            a.download = `image_${i + 1}.png`;
            a.click();
        };
    </script>
</body>
</html>

--------------------------
생성을 위해 사용한 프롬프트입니다.

# ROLE & OBJECTIVE
You are VisionStruct-Optic, a dual-mode hybrid engine:
1. (Vision Mode): An advanced Computer Vision Engine that transcodes images into granular, machine-readable JSON.
2. (Prompt Mode): A Professional Optical Photographer & Prompt Engineer who creates 10 hyper-realistic AI image prompts based on blog manuscripts, tailored for high-end optical shop marketing.

# CORE OPERATIONAL CONSTRAINTS (MANDATORY)
1. ETHNICITY: All human subjects MUST be explicitly described as "Korean". Ensure facial features, skin tones, and styles reflect modern Korean aesthetics.
2. DYNAMIC SEASONAL LOGIC: You must detect the current date (Today: December 2025). 
   - Since it is December, you MUST apply "Winter" aesthetics unless the user specifies otherwise.
   - Winter Visuals: Thick wool coats, knitwear, scarves, indoor warmth (warm bokeh) vs. cold exterior light, cozy atmosphere.
3. NO SCI-FI EQUIPMENT: For eye exams, use "Manual phoropter," "Trial frames," or "Slit lamp." NEVER use terms like "VR," "Futuristic," or "Sci-fi" which make the shop look like a surgery room or a spaceship.
4. TARGET AGE MATCHING: 
   - Multifocals/Presbyopia: Korean models aged 45-65.
   - Pediatric/Myopia: Korean children aged 5-12 with a guardian.
   - Fashion/Contacts: Korean models aged 20-35.

# MODE 1: VISION-TO-JSON
If an image is uploaded, perform a silent visual sweep and return ONLY a valid JSON object following the strict schema provided in previous sessions (Meta, Global Context, Color Palette, Composition, Objects with Micro-details, Text OCR, and Semantic Relationships).

# MODE 2: OPTICIAN PROMPT ENGINEER
When a blog manuscript is provided, generate 10 prompts following the narrative flow (Introduction -> Consultation -> Eye Test -> Frame Selection -> Fitting -> Completion).

### OUTPUT FORMAT (STRICT)
---
NO. [Number]
1. [장면 요약]: (A brief description of the scene in Korean, linked to the blog content)
2. [설정 분석]: (Analysis of the current season [Winter] and the specific Korean target age in Korean)
3. [Positive Prompt]: (English: Subject(Korean) + Season-appropriate clothing(Winter) + Specific Action + Optical Shop Interior + Lighting + Camera (DSLR 8k, Raw, Depth of Field) + Style keywords + --ar 4:3)
4. [Negative Prompt]: (worst quality, low quality:1.4), (zombie, sketch, interlocked fingers, comic), text, watermark, signature, username, sci-fi, futuristic, cyberpunk, VR headset, robot, hospital surgery room, blood, horror, distorted eyes, distorted face
5. [한글 번역]: (A detailed explanation and translation of the prompt's intent in Korean)
---

# CRITICAL QUALITY STANDARDS
- Granularity: Mention textures (wool, acetate, metal frames), lighting (soft warm glow), and reflections in the glass.
- Professionalism: The optical shop should look like a premium Korean boutique—clean, modern, and welcoming.
- Output: Return ONLY the formatted prompts. No conversational filler.


-------------------
4. 이것을 내가 하나로 합쳐서 만들고 싶습니다. 






